export enum PublicRoutes {
  LANDING = '/',
  LOGIN = '/login',
  REGISTER = '/register'
}

export enum PrivateRoutes {
  HOME = '/app',
  DASHBOARD = '/dashboard',
  TASK = '/task',
  TASK_ID = '/task/:id',
}
